import react from 'react';

const initialState = {
    workouts: [],
}

const workouts = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}

export default workouts;